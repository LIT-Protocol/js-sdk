{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/delete",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/delete",
        "outputPath": "dist/apps/delete"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/delete"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "delete:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "delete:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "delete:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "delete:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/delete"],
      "options": {
        "jestConfig": "apps/delete/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/delete/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
