import { getEthAuthMethodId } from './auth-utils';
//TODO: update with tests of hashing implementation for auth method ids.
describe('auth-utils', () => {
    it('place holder', () => {
        expect(1).toEqual(1);
    });
});
