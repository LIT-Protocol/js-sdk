// Legacy entrypoint kept for internal imports.
// All logging is now centralized in @lit-protocol/logger.
export { logger } from '@lit-protocol/logger';
export type {
  Logger,
  LogLevel,
  LogTransport,
  LogEntry,
} from '@lit-protocol/logger';
