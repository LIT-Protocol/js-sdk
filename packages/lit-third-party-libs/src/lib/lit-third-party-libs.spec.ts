import { litThirdPartyLibs } from './lit-third-party-libs';

describe('litThirdPartyLibs', () => {
  it('should work', () => {
    expect(litThirdPartyLibs()).toEqual('lit-third-party-libs');
  });
});
